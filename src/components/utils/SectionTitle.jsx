

const SectionTitle = ({title}) => {
    return (
        <div>
            <h2 className='text-4xl md:text-6xl font-bold text-slate-700 text-center'>{title}</h2>
        </div>
    );
};

export default SectionTitle;